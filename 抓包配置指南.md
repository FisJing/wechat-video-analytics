# 微信视频数据抓包配置指南

## 一、配置手机代理

### 步骤1：在资产13（192.168.31.163）上设置代理

1. 打开 **设置** → **WLAN**
2. 长按当前连接的WiFi网络（192.168.31.xxx）
3. 选择 **修改网络** 或 **网络详情**
4. 找到 **代理** 设置，选择 **手动**
5. 输入：
   - **代理服务器主机名**: `192.168.31.224`
   - **代理服务器端口**: `8080`
6. 保存设置

### 步骤2：安装HTTPS证书

**方法1：通过浏览器安装（推荐）**
1. 在手机浏览器中打开：`http://mitm.it`
2. 点击 Android 图标下载证书
3. 下载后，打开证书文件
4. 输入证书名称（如：mitmproxy）
5. 选择用途：VPN和应用
6. 点击确定完成安装

**方法2：通过ADB安装**
```bash
# 在电脑上执行
adb -s 192.168.31.163:5555 shell "am start -a android.intent.action.VIEW -d http://mitm.it"
```

### 步骤3：验证配置

1. 在手机上打开浏览器，访问任意HTTPS网站
2. 如果能正常访问，说明证书安装成功
3. 如果提示证书错误，请重新安装证书

## 二、开始抓包

### 方式1：自动抓包（推荐）

保持mitmproxy运行，然后在手机上：
1. 打开微信
2. 进入视频号
3. 点击视频分析
4. 数据会自动被抓取

### 方式2：手动触发

在电脑上执行：
```bash
node extract-video-data.js
```

## 三、查看抓包数据

抓包数据保存在：`video_traffic.flow`

### 解析数据

运行解析脚本：
```bash
node parse-flow.js
```

输出JSON格式的视频数据。

## 四、注意事项

1. **证书有效期**：mitmproxy证书长期有效
2. **代理关闭**：使用完毕后，记得关闭手机代理设置
3. **隐私安全**：只在测试设备上使用，不要在生产环境使用
4. **网络要求**：手机和电脑必须在同一局域网

## 五、故障排查

### 问题1：无法访问网站
- 检查代理设置是否正确
- 检查防火墙是否允许8080端口
- 确认电脑IP地址正确

### 问题2：HTTPS网站提示不安全
- 证书未安装或安装失败
- 重新下载并安装证书

### 问题3：抓不到数据
- 确认mitmproxy正在运行
- 检查手机是否使用了代理
- 确认应用使用了网络请求

## 六、停止抓包

在mitmproxy窗口按 `Ctrl+C` 停止
